const Records = (props) => {
    const {showingCurrentBeers} = props;

    return (
        <>
            <p>Showing {showingCurrentBeers} different beers</p>
        </>
    );
};

export default Records;
